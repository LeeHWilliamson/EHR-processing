add labs to patient schemaX
consider adding internal ids as availableX
check that dates and times are included in all relevant categoriesX
create separate jsons for each patient, make sure each is formatted in readable wayX

create ids for every single patient entity
make sure all ids (entity, document, log) are unique between runs
create category level schemas for each document typeX
create document schemaX
Create doc creation log schema
Create OCR result log schema
design templates (probably in word) for versions of each doc type
create json templates for each version of a document type (e.g. visit_summary_a vs visit_summary_b)

create document ground truths
create patient->document mapping ground truth
