'''
This script will reconstruct patient records from document metadata (i.e. mapping ground truth), raw text version of document (regex parsing), and ocr output (also via regex)
It will then create a record comparing the records constructed by the 3 methods, and their accuracy

'''
